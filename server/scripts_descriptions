Check-effect.py:
	- gettotal(conn) : return the number of website in the storage databases
	- getdata(name, restriction_str, conn, total) : get data from the storage databases and return the accuracy as percentage,mean and stf of the regions
	- effect(restriction) : format input restriction on the found regions and print the filters performance
	
Hit-verifier.py:
	- Basically a script that translates domain names of localized versions into the "base" domain.
	- I don't think we need to re-run this since it seems the "hits" are already in the databases

ROC.py:
	- 
	- 
	
Check-classifier.py:
	- This script calculates the similarity scores for the result pages.
	
